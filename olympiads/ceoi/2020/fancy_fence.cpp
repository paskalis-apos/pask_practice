#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> heights(n);
    vector<int> widths(n);
    for(auto &x : heights){ 
        cin >> x;
    }
    for(auto &x : widths) {
        cin >> x;
    }

    
}