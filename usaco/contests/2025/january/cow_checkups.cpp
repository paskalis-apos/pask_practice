#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    vector<int> cowrow(n);
    vector<int> wantrow(n);
    
    for(auto &x : cowrow) {
        cin >> x;
    }
    for(auto &x : wantrow) {
        cin >> x;
    }

        
}