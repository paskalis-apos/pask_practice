#include<bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int n;
    cin >> n;
    vector<int> v(n);
    for(auto &x : v) {
        cin >> x;
    }   
    vector<int> v2(n);
    for(auto &x : v) {
        cin >> x;
    }
    stack<pair<int,int>> maxsegs;
    for(int i = 0 ;i < n;i++) {
        
    }
    
}


int main() {
    int t;
    cin >> t;
    for(int i = 0;i < t ;i++) {
        solve();
    }
}